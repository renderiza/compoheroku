= form_for @user, url: export_scene_path, method: :post do |f|
	.form-group
		= f.label :export
		= f.select :board_id, options_from_collection_for_select(current_user.boards, "id", "name"), class: 'form-control'
	.actions
		= f.submit class: 'btn btn-primary'